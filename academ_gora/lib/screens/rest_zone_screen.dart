import 'package:flutter/material.dart';

class RestZoneScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Академгора")),
      body: Center(
          child: Text("Зона отдыха и детского досуга")
      ),
    );
  }
}