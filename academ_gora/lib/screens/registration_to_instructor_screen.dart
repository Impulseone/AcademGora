import 'package:flutter/material.dart';

class RegistrationToInstructorScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("АкадемГора")),
      body: Center(
          child: Text("Запись к инструктору")
      ),
    );
  }
}